export default class Bar {  constructor(tel) {    this.ThumbBtn = tel.ThumbBtn;    this.VolumeBarLine = tel.VolumeBarLine;    this.timeBarLoad = tel.timeBarLoad;    this.timeBarLine = tel.timeBarLine;    this.timeBarThumb = tel.timeBarThumb;  }  set(percentage) {    this.VolumeBarLine.style["width"] = percentage * 100 + "%";  }  setLoadProgress(percentage) {    this.timeBarLoad.style.width = percentage * 100 + "%";  }  setPlayed(percentage) {    this.timeBarLine.style.width = percentage * 100 + "%";  }}