class TimeBar {  constructor(player) {    this.player=player;    this.timeBarLoad = player.tel.timeBarLoad;    this.timeBarLine = player.tel.timeBarLine;    this.timeBarThumb = player.tel.timeBarThumb;    this.enableloadingCheck=false;    this.initLoading()  }  //https://www.cnblogs.com/1wen/p/5973468.html  initLoading(){    let checkTime = 0;    let checkLastTime = 0;    let loadStart=false;    this.check = setInterval(()=>{      if (!this.enableloadingCheck) {        return false      }       checkLastTime=this.player.players.currentTime;       const paused=!this.player.paused        if (!loadStart&&checkTime===checkLastTime&&paused){          this.player.container.classList.add('player-loading');          loadStart=true        }      if (loadStart          && checkTime > checkLastTime          && !paused) {        this.player.container.classList.remove('player-loading');        loadStart = false;      }       checkTime=checkLastTime;    }, 300);  }  enableloading(){    this.enableloadingCheck=true  }  disableloading (type) {    this.enableloadingCheck=false  }}export default TimeBar;